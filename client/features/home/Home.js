import React from 'react';
import { useSelector } from 'react-redux';
import Carousel from '../carousel/Carousel';
const Home = (props) => {
  return (
    <div>
      <Carousel />
    </div>
  );
};

export default Home;
